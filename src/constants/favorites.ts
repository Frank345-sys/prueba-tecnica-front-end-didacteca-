/** Número máximo de personajes favoritos permitidos. */
export const MAX_FAVORITES = 5

/** Clave de `localStorage` para persistir el orden de favoritos. */
export const FAVORITES_STORAGE_KEY = 'rick-morty-favorites'
